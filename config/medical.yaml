defaults:
  - _self_

dataset_path: "dummy_dataset"
image_shape: [512, 512]
batch_size: 1
num_context_views: 2

# Model configuration (minimal defaults, adjust as needed)
model:
  encoder:
    name: "epipolar" # or whatever the default encoder is
    # ... other model params
